- @post.comments.each do |comment|
  .col-md-12
    = comment.body
    %span.user
      = comment.user.email
  - if comment.user_id == current_user.id
    .col-md-12
      .btn-group.pull-right
        = link_to edit_post_comment_path(comment.post, comment) do
          %span.glyphicon.glyphicon.glyphicon-edit Editer
        = link_to [comment.post, comment], method: :delete, data: {confirm: "Le commentaire sera définitivement Supprimer"} do
          %span.glyphicon.glyphicon-trash Supprimer
    %br
  %hr